fastapi>=0.110.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1
email-validator>=2.1.0
alembic>=1.13.0
uvicorn[standard]>=0.29.0
sqlalchemy>=2.0.0
pydantic>=2.6.0
python-multipart>=0.0.9
httpx>=0.27.0
python-dotenv>=1.0.0

# OCR / AI
openai>=1.20.0         # for GPT-4o Vision receipt parsing
google-genai>=1.0.0    # for Gemini Vision receipt parsing (free tier)
openpyxl>=3.1.0        # for Excel export
groq>=0.9.0            # for Groq Vision receipt parsing (free tier, no card)

# Barcode decoding
opencv-python-headless>=4.5.2
Pillow>=10.0.0
numpy>=1.24.0

# Optional: local OCR fallback
# pytesseract>=0.3.10
# pillow>=10.0.0

# PostgreSQL driver (required for Railway / production)
psycopg2-binary>=2.9.9

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
